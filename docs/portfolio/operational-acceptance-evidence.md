# 운영 인수 증빙

검증일: 2026-09-09. 아래 표는 구현 코드와 실제 실행 증거를 구분합니다.

## 인수 기준

| 기준 | 구현·검증 증거 | 상태 |
|---|---|---|
| 세 역할의 동일 업무 기록 | 거래처 주문 → 관리자 확정 → 창고 부분 출고 → 반품 검수 → 정산 마감 → 입금 배분 Playwright E2E | 통과 |
| 동시 주문 재고 초과 방지 | 행 잠금 기반 예약과 동시 승인·추가 배정 PostgreSQL 통합 테스트 | 통과 |
| 중복 요청 단회 반영 | 주문, 출고, 반품, 입금 명령의 `requestId` replay와 원장 건수 검증 | 통과 |
| 커밋 후 응답 유실·API 재시작 | TCP 프록시 응답 폐기 후 API 인스턴스 교체, 같은 주문 `requestId` 재전송과 단일 기록 검증 | 통과 |
| 부분 출고와 잔량 처리 | 여러 차례 출고, 미출고 취소 요청·승인·반려, 추가 입고 재배정 테스트 | 통과 |
| 반품 수량·재고·금액 연결 | 원출고 기준 상한, 정상 재고 복귀, 불량 처리, 반품 차감 원장 테스트 | 통과 |
| 정산·입금 정정 가능성 | 월 초안·마감, 마감 후 다음 달 차감, 입금 배분 취소·재배분 테스트 | 통과 |
| 거래처 격리와 역할 권한 | 비로그인·거래처·창고·MFA 전후 운영자 9개 HTTP 권한 행렬 | 통과 |
| DB·역할 환경 분리 | 개발·테스트·migration 4개 connection과 교차 환경 접근 거절 | 통과 |
| 운영 데이터 정합성 | API live/ready, 22개 table count, 7개 원장·잔액 정합성 검사 | 통과 |
| 초기 데이터 이관 | CSV preview, transaction 적용, 중복 hash와 충돌 처리 DB·브라우저 검증 | 통과 |
| 백업 복원 | 임시 PostgreSQL DB 복원 후 table count와 정합성 비교 기록 | 통과 |
| 역할별 화면 품질 | 관리자·창고·거래처 16개 화면, 데스크톱 1440×900과 모바일 390×844 Playwright 검사 | 32/32 통과 |
| 목표 규모 지속 성능 | 주문 100,000건·주문행 1,000,000건·30분 workload | 미실행 |

## 최신 운영 스모크

`npm.cmd run ops:acceptance-smoke` 실행 결과:

- 동시 작업자: 30
- 요청: 300
- 전체 p50: 78.7ms
- 전체 p95: 96.0ms
- 최대: 122.3ms
- 예기치 않은 오류: 0건
- 권한 행렬: 9/9 통과

이 결과는 현재 개발 DB와 데모 데이터에서 수행한 로컬 회귀 스모크입니다. 목표 규모 본 시험 결과로 해석하지 않습니다.

## 최신 운영 준비 검사

- `admin:verify`: 초기 관리자와 MFA encryption key 준비 확인
- `db:verify`: 4개 DB connection과 역할 권한, 교차 환경 접근 거절 확인
- `ops:verify`: API live/ready, 22개 table count, 7개 integrity check 통과
- 관리자 재고·반품 페이지 변경 관련 실제 DB 테스트: 20/20 통과
- `build:api`, `build:web`: 통과

## 아키텍처 다이어그램 검증

- specification SHA-256: `7c167f7f5abbd2e086440958e8e2148ca6f27f028173362a6f62cb0a46b3e840`
- artifact SHA-256: `4a7b220df9571ab5560dd2ada707c94543698bc2e89c6bc83a484f9b489c5282`
- Archify showcase: 9/9 checks, composition error 0, warning 0
- Chrome automated browser: 1440×900, 1600×1000, 1920×1080, 2048×1320 모두 overflow 없음
- 라이트 모드 1440×900 캡처를 직접 확인해 구성 요소, 관계 라벨과 증빙 카드의 겹침이 없음을 확인했습니다.

Archify의 viewer 자체 UI는 한국어 locale을 제공하지 않아 고정 조작 UI는 영어로 표시됩니다. 작성한 시스템 내용은 한국어입니다.
