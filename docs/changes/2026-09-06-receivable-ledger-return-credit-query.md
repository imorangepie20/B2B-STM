# 반품 차감 포함 미수 원장 조회

출고 미수 조회가 `shipment_lines` 내부 조인을 사용해 `return_credit` 음수 원장을 누락하던 문제를 수정했다. 출고와 반품 차감의 원천을 각각 외부 조인하고 상품 정보를 병합해, 정산 화면과 API가 양수 출고·음수 반품 차감을 모두 표시한다.

반품 통합 테스트는 -30,000원 차감 원장이 `/api/admin/receivables` 응답에 포함되는지 검증한다.
