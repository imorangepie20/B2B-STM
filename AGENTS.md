# 프로젝트 공통 작업 규칙

이 규칙은 B2B-STM 저장소 전체에 적용합니다. 하위 디렉터리의 추가 지침도 확인합니다.

## 작업 전 확인

매 세션 시작과 중요 변경 전에 다음 문서를 읽습니다.

1. `docs/project-rules/core-principles.md`
2. `docs/project-rules/doc-access-order.md`
3. `docs/overview/current-development-context.md`

구현·디버깅 전에는 개요와 현재 상태 → 아키텍처와 도메인 → 업무 흐름과 변경 이력 → 구현과 테스트 순서로 확인합니다.

## 변경 원칙

- 증상 우회가 아닌 근본 원인을 수정합니다.
- `customer -> order -> reservation -> shipment -> settlement -> payment`와 `receipt -> inventory`, `return -> inventory/settlement`의 현재 구현 관계에 미칠 영향을 확인합니다. 후속 피킹·검수·실사·환불 관계를 추가하면 관련 문서도 함께 갱신합니다.
- 요청에 필요한 최소 변경과 관련 검증을 수행합니다. 기존 사용자 변경을 되돌리지 않습니다.
- 기획 확정 후 제품 구현을 시작합니다. 확정 사항과 제안을 구분하며 문서화 요청을 구현 승인으로 해석하지 않습니다.
- `C:\Users\jowoo\alpahMomega`는 독립된 부동산 프로젝트입니다. 코드·데이터·비밀값·운영 환경을 혼용하지 않습니다.
- `SDTPL_ADM/`은 B2B 웹 UI의 기준 테마 원본입니다. 원본은 수정하지 않되, 사용자가 2026-09-07에 허용한 범위에서 필요한 UI 코드와 자산을 `apps/web`으로 복사해 사용할 수 있습니다. 신규·개편 UI는 이 테마의 토큰, 레이아웃, 컴포넌트 체계를 따르며 별도 디자인 체계를 만들지 않습니다. 원본의 `.git`과 비밀값은 가져오지 않습니다.
- 차트·데이터 그리드·달력·대화상자처럼 동작과 접근성 계약이 있는 UI는 검증된 라이브러리와 프로젝트의 정식 컴포넌트로 구현합니다. CSS나 정적 마크업으로 외형만 흉내 내 완료 처리하지 않으며, 데이터 연결·상호작용·키보드 접근·반응형 동작을 실제 브라우저에서 검증합니다.

## 문서화

- 의미 있는 아키텍처·업무·의사결정·구현 변경을 `docs/`에 기록합니다.
- 변경 이유, 문제의 근본 원인(해당하는 경우), 변경 내용, 검증 결과와 미검증 항목을 남깁니다.
- 일반 설명은 한국어로, 코드 식별자·경로·명령어·API endpoint·URL·환경 변수는 원문으로 작성합니다.
- 검증하지 않은 기능을 완료로 표시하지 않습니다.
